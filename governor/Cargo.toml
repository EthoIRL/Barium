[package]
name = "governor"
version = "0.1.0"
edition = "2021"

[dependencies]
strum = { version = "0.26.3", features = ["derive"] }
prost = "0.13.3"
uuid = { version = "1.11.0", features = ["v4"] }

[build-dependencies]
prost-build = "0.13.3"
