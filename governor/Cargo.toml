[package]
name = "governor"
version = "0.1.0"
edition = "2021"

[dependencies]
threadpool = "1.8.1"
strum = { version = "0.26.3", features = ["derive"] }
prost = "0.13.3"

[build-dependencies]
prost-build = "0.13.3"
